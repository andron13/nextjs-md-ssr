import TestContent from '../../constants/testContent';

const FirstScreen = () => {
  return (
    <div className=" my-4 bg-teal-50 px-2 pb-0 pt-0">
      <TestContent />
    </div>
  );
};

export default FirstScreen;
