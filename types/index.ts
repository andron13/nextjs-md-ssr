export type LinkObj = {
  title: string;
  path: string;
};

export type BreadcrumbObj = {
  label: string;
  href: string;
};

export type MetadatObj = {
  title: string;
  subtitle: string;
  date: string;
  author: string;
  category: string;
  weight: string;
  slug: string;
};
